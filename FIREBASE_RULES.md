# Firebase Security Rules

## Firestore Rules
These rules ensure that users can only read and write their own documents, while allowing read access for others if necessary for public profiles (optional, strict by default here).